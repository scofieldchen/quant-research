## 问题

1. 对于失败任务，仅处理一次，这是最优选择吗？在第二轮下载中也有可能产生失败任务，应该设置最大重试次数，将补漏的逻辑封装到循环中。
2. run_update方法似乎并没有利用开始日期和结束日期，即便用户指定了参数，仍然会从当前数据库的最后一天开始。
3. 增量更新数据时，假设更新某交易对过去7天的数据，但是有一天下载失败（api，因为仓库调用失败将回退到api），这将导致数据缺失，这种情况也应该被记录为失败任务，需要重新处理该交易对的更新任务，只需要重新下载那一天并更新当月数据。有更好的处理方法吗？


简化`_download_month_task`
